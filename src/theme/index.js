import typography from "./typography"
import colors from "./colors"
import fonts from "./fonts"
import maxWidth from "../foundation/maxWidth"

const theme = {
   typography,
   colors,
   fonts,
   maxWidth
}

export default theme